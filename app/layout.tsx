import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "Konkani Speech Dataset Home Page",
  description: "This website is used to collect speech and sentences in Roman Konkani and Devanagari Konkani. It also takes the English translation of the Konkani sentences for the translation dataset. ",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  );
}
